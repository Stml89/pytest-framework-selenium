[pytest]
minversion = 7.0
log_cli=true
log_level=INFO
log_format = %(asctime)s::%(filename)s:%(lineno)s:: %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
addopts = --capture=sys -rxX
markers =
  smoke: Run the smoke test suite
  regression: Run the regression test suite